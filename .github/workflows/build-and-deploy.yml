name: GloirePay — Pipeline Souverain

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  audit-et-simulation:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Simulation de Sécurité (Audit nœud)
        env:
          ALCHEMY_API_KEY: ${{ secrets.ALCHEMY_API_KEY }}
        run: |
          echo ">>> [AUDIT] Vérification de la souveraineté du nœud..."
          curl -s -X POST "$ALCHEMY_API_KEY" \
            -H "Content-Type: application/json" \
            --data '{"jsonrpc":"2.0","method":"eth_blockNumber","params":[],"id":1}'
          echo ">>> [SUCCESS] Audit validé."

  deploy:
    needs: audit-et-simulation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
          
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Installation des dépendances
        run: pip install web3

      - name: Exécution du transfert souverain
        env:
          POLYGON_PRIVATE_KEY: ${{ secrets.POLYGON_PRIVATE_KEY }}
          ALCHEMY_API_KEY: ${{ secrets.ALCHEMY_API_KEY }}
        run: python execute_transfer.py
